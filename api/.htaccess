<IfModule mod_rewrite.c>
  RewriteEngine On

  # Serve existing files directly
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]

  # Everything else routes to the front controller
  RewriteRule ^ index.php [L]
</IfModule>
